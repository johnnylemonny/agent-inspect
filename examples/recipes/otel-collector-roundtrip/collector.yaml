receivers:
  otlp:
    protocols:
      http:
        endpoint: 127.0.0.1:4318

exporters:
  file:
    path: ./collected-traces.json
    format: json

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
